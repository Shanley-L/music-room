# Adresse de base du backend (REST + Socket), inlinée au build Expo.
# Défaut si absente : http://localhost:3000 (non-Android) / http://10.0.2.2:3000 (émulateur Android).
EXPO_PUBLIC_API_URL=http://localhost:3000